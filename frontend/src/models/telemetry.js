export default class Telemetry {
    constructor(name, project, description) {
      this.time = time;
      this.device_id = device_id;
      this.key_id = key_id;
      this.value = value;
    }
  }