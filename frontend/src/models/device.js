export default class Device {
    constructor(name, project, description, uid) {
      this.name = name;
      this.project = project;
      this.description = description;
      this.uid = uid;
    }
  }